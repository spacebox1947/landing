import { TrimNewsOutletNamePipe } from './trim-news-outlet-name.pipe';

describe('TrimNewsOutletNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimNewsOutletNamePipe();
    expect(pipe).toBeTruthy();
  });
});
